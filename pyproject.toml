[project]
name = "Leetify-Data-Crawler"
version = "0.1.0"
description = "Data Crawler for Leetify, crawls data starting from one specific player."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pandas>=3.0.2",
    "python-dotenv>=1.2.2",
    "requests>=2.33.1",
]
